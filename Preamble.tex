% =========================
% Preamble.tex (clean + robust)
% =========================

% Language
\usepackage[american]{babel}
\usepackage{csquotes}

% Page layout
\usepackage[a4paper,left=3cm,right=3cm,top=2.5cm,bottom=2.5cm]{geometry}

% Paragraph formatting (no indentation + vertical space between paragraphs)
\usepackage{parskip}

% Better typography
\usepackage[final]{microtype}

% Math
\usepackage{amsmath}
\usepackage{amssymb}

% Figures
\usepackage{graphicx}
\graphicspath{
    {Inputs/Figures/}
    {Inputs/Pictures/}
}

% Tables
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{adjustbox}

% Bibliography (APA 7)
\usepackage[backend=biber,style=apa]{biblatex}
\DeclareLanguageMapping{american}{american-apa}
\addbibresource{References.bib}

% Hyperlinks (load last)
\usepackage[hidelinks]{hyperref}

% Helps LaTeX avoid spacing warnings
\emergencystretch=3em
